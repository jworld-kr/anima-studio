# 대시보드 디자인 구조

## 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│                     헤더 (로그아웃)                        │
├──────────────┬──────────────────────────────────────────┤
│              │                                          │
│   좌측       │           메인 콘텐츠 영역                │
│  사이드바    │  (탭 또는 메인 뷰)                       │
│              │                                          │
│  • 채널1     │  ┌──────────────────────────────────┐  │
│  • 채널2     │  │  [세계관] [Thread] [히스토리]    │  │
│  • 채널3     │  └──────────────────────────────────┘  │
│    (현재)    │                                          │
│              │  현재 선택된 탭의 콘텐츠                 │
│  [+ 채널]    │                                          │
│              │                                          │
└──────────────┴──────────────────────────────────────────┘
```

---

## 1. 좌측 사이드바

### 구조
```
┌─────────────────┐
│  채널 목록      │
├─────────────────┤
│ 📍 채널1        │  ← 현재 선택
│ ○ 채널2        │
│ ○ 채널3        │
├─────────────────┤
│ [+ 새 채널]    │
└─────────────────┘
```

### 기능
- 채널명 클릭 → 해당 채널 대시보드로 전환
- 현재 선택된 채널 하이라이트 (파란색/굵은 글씨)
- 채널 정보 표시 (썸네일 + 이름)
- "+ 새 채널" 버튼으로 채널 추가

---

## 2. 메인 콘텐츠 영역

### 헤더 (채널 정보 + 탭)
```
┌─────────────────────────────────────────┐
│ 고독이 (채널명)                          │
├─────────────────────────────────────────┤
│ [세계관] [Thread] [히스토리]            │
└─────────────────────────────────────────┘
```

### 각 탭 콘텐츠

#### Tab 1: 세계관
```
┌─────────────────────────────────────────┐
│ 세계관 설정                              │
├─────────────────────────────────────────┤
│ 좌측: 에디터 폼                         │
│ 우측: 시스템 프롬프트 미리보기          │
│                                         │
│ [저장] [Thread 생성으로]               │
└─────────────────────────────────────────┘
```

#### Tab 2: Thread
```
┌─────────────────────────────────────────┐
│ Thread 생성                              │
├─────────────────────────────────────────┤
│                                         │
│ 상태: 입력 → 훅 선택 → 게시물 편집    │
│                                         │
│ [생성하기] → [훅선택] → [편집/저장]   │
│                                         │
│ 좌측: 입력 폼                          │
│ 우측: 결과 표시                        │
│                                         │
│ [저장 및 히스토리로] [임시저장]        │
└─────────────────────────────────────────┘
```

#### Tab 3: 히스토리
```
┌─────────────────────────────────────────┐
│ 콘텐츠 히스토리                         │
├─────────────────────────────────────────┤
│ 필터: [전체] [임시저장] [예약] [발행]  │
│ 정렬: [최신순] [오래된순]              │
├─────────────────────────────────────────┤
│ • 주제1  | 임시저장 | 3일 전            │
│   [편집] [삭제]                         │
│                                         │
│ • 주제2  | 발행됨  | 5일 전             │
│   [보기] [삭제]                         │
│                                         │
│ • 주제3  | 예약됨  | 내일 2시          │
│   [수정] [취소] [삭제]                 │
└─────────────────────────────────────────┘
```

---

## 3. 페이지 라우팅

### 현재 구조 (변경 전)
```
/channels
/channels/new
/channels/[id]/worldbuilding
/channels/[id]/thread/create
/channels/[id]/history
```

### 변경 후 구조
```
/dashboard/[channelId]
  └ ?tab=worldbuilding (기본값)
  └ ?tab=thread
  └ ?tab=history

/channels (채널 관리 페이지)
  └ /new (새 채널 생성)
```

---

## 4. 컴포넌트 구조

```
app/
├── components/
│   ├── Sidebar.tsx              (좌측 사이드바)
│   ├── DashboardLayout.tsx      (대시보드 레이아웃)
│   ├── TabNavigation.tsx        (탭 네비게이션)
│   ├── WorldBuildingTab.tsx     (세계관 탭)
│   ├── ThreadTab.tsx            (Thread 탭)
│   ├── HistoryTab.tsx           (히스토리 탭)
│   └── ...
├── app/
│   ├── dashboard/
│   │   └── [channelId]/
│   │       └── page.tsx         (메인 대시보드)
│   ├── channels/
│   │   ├── page.tsx             (채널 관리)
│   │   └── new/page.tsx         (채널 생성)
│   └── ...
```

---

## 5. 상태 관리 흐름

### 사이드바
```
사이드바에서 채널 클릭
  ↓
URL 변경: /dashboard/[channelId]
  ↓
메인 영역 자동 갱신 (선택된 채널의 데이터 로드)
```

### 탭 전환
```
탭 클릭 (세계관/Thread/히스토리)
  ↓
URL 변경: /dashboard/[channelId]?tab=xxx
  ↓
해당 탭 콘텐츠 표시
```

### 데이터 흐름
```
localStorage
  ├── 채널 목록 (사이드바)
  ├── 현재 선택된 채널 정보 (메인 헤더)
  ├── 세계관 설정 (세계관 탭)
  ├── 생성된 콘텐츠 (히스토리 탭)
  └── Thread 임시 저장 (Thread 탭)
```

---

## 6. UI/UX 개선

### 색상 구분
- **현재 선택 채널**: 파란색 배경
- **활성 탭**: 파란색 텍스트, 하단 테두리
- **상태 배지**:
  - 임시저장: 회색
  - 예약됨: 노란색
  - 발행됨: 초록색

### 반응형
- 데스크톱: 사이드바 고정, 메인 영역 넓음
- 태블릿: 사이드바 접기/펼치기
- 모바일: 사이드바 슬라이드 아웃

---

## 7. 구현 순서

1. **DashboardLayout 만들기** (사이드바 + 탭)
2. **Sidebar 컴포넌트** (채널 목록, 선택)
3. **TabNavigation 컴포넌트** (탭 전환)
4. **기존 탭들을 컴포넌트로 분리** (WorldBuildingTab, ThreadTab, HistoryTab)
5. **라우팅 정리** (/dashboard/[channelId] 통일)
6. **스타일링** (Tailwind)

---

## 8. 예상 결과

✅ 한 곳에서 모든 기능 사용 가능
✅ 채널 전환 빠름 (새로고침 없음)
✅ 뒤로가기 필요 없음
✅ 정보 구조가 명확함
✅ 모바일도 지원 가능
